% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consolidate.R
\name{consolidate}
\alias{consolidate}
\title{Consolidate staged collections into the canonical Parquet store}
\usage{
consolidate(
  staging_dir = "staging",
  data_dir = "data",
  archive_dir = file.path(data_dir, "archive"),
  retention_months = 12L,
  reference_date = Sys.Date(),
  run_ts = Sys.time()
)
}
\arguments{
\item{staging_dir}{Directory holding the collectors' output. Absent
directory or absent files are tolerated.}

\item{data_dir}{Directory holding the canonical Parquet store; the
checkout of the orphan \code{data} branch in CI. Created when missing.}

\item{archive_dir}{Directory for the permanent monthly rollups. Defaults
to \verb{archive/} inside \code{data_dir}.}

\item{retention_months}{Length of the rolling daily window, in months.}

\item{reference_date}{Date the rolling window is measured back from.
Exposed for testing and for deterministic reruns.}

\item{run_ts}{Timestamp recorded on the manifest rows this call writes.}
}
\value{
Invisibly, a tibble with one row per canonical table and the
columns \code{table}, \code{staging_rows}, \code{rows_before}, \code{rows_after},
\code{rollup} (\code{"ok"}/\code{"failed"}/\code{"skipped"}), \code{rollup_rows}, \code{pruned},
\code{rows_pruned} and \code{written}.
}
\description{
The single write path into the canonical data store (the \code{consolidate}
job). In one call, and in this order:
\enumerate{
\item \strong{Upsert} every table found in \code{staging_dir} into \code{data_dir}, by its
natural key. Last-write-wins everywhere except
\code{gh_traffic}, which merges by \code{max()} per key.
\item \strong{Roll up} each table to calendar months into
\verb{data_dir/archive/<table>_monthly.parquet}.
\item \strong{Prune} each table to a rolling 12-month window -- and only then,
and only for tables whose rollup succeeded in this same call.
\item \strong{Record} one \code{run_manifest} row per source processed, with a
sanitised, truncated message.
}
}
\details{
\strong{Missing input is not an error.} A staging table that is absent simply
means its collector did not run (or failed) in this execution; the
corresponding canonical table is left untouched and the remaining tables
are still consolidated. An absent \code{data_dir} is created, and an
absent canonical table is treated as an empty table. The function is
therefore safe to call on a completely empty checkout.

\strong{Idempotence.} Running twice with the same staging input produces the
same canonical tables and the same row counts: the upsert is by
key, the rollup upserts the same monthly aggregate, and the prune is a
function of the data and the reference date only.

\strong{Retention gate.} The prune is not merely ordered after the rollup; it
is conditional on it. \code{gh_traffic} is irrecoverable -- the GitHub API
retains 14 days -- so if its rollup throws, its daily rows are the only
surviving copy of the period and are kept. The outcome per table is
returned, which is what \code{tests/testthat/test-retention-order.R} asserts
on.
}
\examples{
\dontrun{
collect_cran()
consolidate(staging_dir = "staging", data_dir = "data")
}

# Safe on an empty tree: nothing staged, nothing stored.
tmp <- tempfile()
dir.create(tmp)
res <- consolidate(
  staging_dir = file.path(tmp, "staging"),
  data_dir = file.path(tmp, "data")
)
nrow(res)
}
\seealso{
\code{\link[=collect_cran]{collect_cran()}}, \code{\link[=collect_github]{collect_github()}}, \code{\link[=collect_academic]{collect_academic()}},
\code{\link[=export_json]{export_json()}}
}
