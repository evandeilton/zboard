# This file is part of the standard testthat infrastructure. It is run
# automatically as part of R CMD check and by devtools::test().
# See SPEC.md tests/ for the required test files.

library(testthat)
library(zboard)

test_check("zboard")
