% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect_academic.R
\name{collect_academic}
\alias{collect_academic}
\title{Collect academic output from OpenAlex}
\usage{
collect_academic(
  from = NULL,
  to = NULL,
  config_path = "config/repos.yml",
  staging_dir = "staging",
  mailto = Sys.getenv("OPENALEX_MAILTO", ""),
  run_ts = Sys.time()
)
}
\arguments{
\item{from}{Optional lower bound on publication date, as an ISO-8601 date
string or a \code{Date}. \code{NULL} (the default) applies no lower bound.}

\item{to}{Optional upper bound on publication date, same forms as \code{from}.}

\item{config_path}{Path to the curated source configuration
(SPEC.md S5).}

\item{staging_dir}{Directory the staging Parquet file is written to.
Created when missing.}

\item{mailto}{Contact address for the OpenAlex polite pool. Defaults to
the \code{OPENALEX_MAILTO} environment variable, and to \code{""} (anonymous
pool) when that is unset.}

\item{run_ts}{Timestamp shared by every manifest row of this run.}
}
\value{
Invisibly, a tibble with the \code{run_manifest} schema: one row for
the \code{academic_works} source. The same row is written to
\code{staging_dir/run_manifest_academic.parquet}.
}
\description{
Collects the \code{academic_works} snapshot of SPEC.md S4.2 -- publications,
preprints, datasets and software attributed to the ORCID iD configured in
\code{config/repos.yml}, with their venue, year, type and accumulated citation
count -- and writes it as \code{staging_dir/academic_works.parquet} for
\code{\link[=consolidate]{consolidate()}}.
}
\details{
\strong{Polite pool.} OpenAlex serves anonymous callers from a slower, shared
pool. Supplying a contact address moves the caller to the polite pool.
The address is a repository secret (SPEC.md S11): it is read from
\code{OPENALEX_MAILTO} as the default of the \code{mailto} argument, is never
hard-coded, and is never written to any output file. When the variable is
unset the collector still runs, unauthenticated, and says so.

\strong{What \code{from}/\code{to} mean here.} \code{academic_works} is a snapshot keyed by
\verb{(snapshot_date, work_id)}, so there is no past state to rebuild. An
explicit window is therefore interpreted as a filter on the \emph{publication
date} of the works to collect (OpenAlex's \code{from_publication_date} /
\code{to_publication_date}), not as a range of snapshots. The default, \code{NULL},
collects the whole body of work in every run.

\strong{Type values.} \code{type} is stored exactly as OpenAlex reports it
(\code{article}, \code{preprint}, \code{dataset}, \code{book-chapter}, ...). SPEC.md S4.2
lists four illustrative values; normalising to that closed set would
discard information the dashboard can display verbatim.

Only the author's own works are collected. No co-author name, identifier
or affiliation is stored (NG-5).
}
\examples{
\dontrun{
Sys.setenv(OPENALEX_MAILTO = "you@example.com")
collect_academic()

# Only works published from 2024 onwards.
collect_academic(from = "2024-01-01")
}
}
\seealso{
\code{\link[=collect_cran]{collect_cran()}}, \code{\link[=collect_github]{collect_github()}}, \code{\link[=consolidate]{consolidate()}}
}
