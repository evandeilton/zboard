% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_json.R
\name{export_json}
\alias{export_json}
\title{Export the canonical Parquet store as JSON for the static dashboard}
\usage{
export_json(
  data_dir = "data",
  output_dir = "site/_data",
  archive_dir = file.path(data_dir, "archive"),
  downloads_window_days = 365L,
  redact_emails = TRUE,
  pretty = FALSE,
  generated_at = Sys.time()
)
}
\arguments{
\item{data_dir}{Directory holding the canonical Parquet store.}

\item{output_dir}{Directory the JSON files are written to. Created when
missing.}

\item{archive_dir}{Directory holding the monthly rollups. Defaults to
\verb{archive/} inside \code{data_dir}.}

\item{downloads_window_days}{Length, in days, of the daily download and
activity series exported (the rolling daily window).}

\item{redact_emails}{Strip e-mail addresses from \code{maintainer} before
publishing. Leave \code{TRUE} unless you have a specific reason not to.}

\item{pretty}{Pretty-print the JSON. \code{FALSE} produces smaller files.}

\item{generated_at}{Build timestamp recorded in every file.}
}
\value{
Invisibly, a tibble with one row per file written and the columns
\code{file}, \code{path} and \code{bytes}.
}
\description{
Reads \verb{data_dir/*.parquet} (and the monthly archives under
\code{archive_dir}) and writes the ten JSON files the Quarto dashboard
consumes into \code{output_dir}: \code{overview.json}, \code{cran_status.json},
\code{cran_downloads.json}, \code{gh_activity.json}, \code{gh_traffic.json},
\code{gh_issues.json}, \code{gh_releases.json}, \code{academic.json},
\code{freshness.json} and \code{meta.json}.

\strong{The complete field-by-field schema of all ten files is documented as a
comment block at the top of \code{R/export_json.R}.} That block is the
contract between this package and the front-end; read it there.
}
\details{
An absent Parquet file is an empty table, not an error, and every array
in the output is emitted even when empty. Calling this on a directory
with no data at all produces ten valid, empty-but-well-formed JSON files,
which is what lets the site build before the first collection has run.

Dates and timestamps are serialised as ISO-8601 strings; no Arrow or R
date type reaches the JSON. Numbers are written at full precision
(\code{digits = NA}).

\code{cran_status$maintainer} is collected from CRAN with an e-mail address
attached. Because these files are published on a public site, the address
is removed by default, leaving the display name.
}
\examples{
# Works on an empty store: every file is written, every array is empty.
tmp <- tempfile()
dir.create(tmp)
out <- export_json(
  data_dir = file.path(tmp, "data"),
  output_dir = file.path(tmp, "json")
)
out$file
jsonlite::fromJSON(file.path(tmp, "json", "meta.json"))$schema_version

\dontrun{
# In the pipeline, against the checked-out `data` branch.
export_json(data_dir = "data", output_dir = "site/_data")
}
}
\seealso{
\code{\link[=consolidate]{consolidate()}}, \code{\link[=make_badges]{make_badges()}}
}
