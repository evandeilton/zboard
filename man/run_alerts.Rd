% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/alert.R
\name{run_alerts}
\alias{run_alerts}
\title{Open, update and close CRAN alert issues}
\usage{
run_alerts(
  repo = Sys.getenv("GITHUB_REPOSITORY"),
  token = Sys.getenv("GITHUB_TOKEN"),
  data_dir = "data",
  label = "alert",
  marker_prefix = "zboard"
)
}
\arguments{
\item{repo}{Target repository as \code{"owner/name"}. Defaults to the
\code{GITHUB_REPOSITORY} environment variable that GitHub Actions sets.}

\item{token}{GitHub token with \code{issues: write}. Defaults to
\code{GITHUB_TOKEN}. \code{""} triggers the dry run described above.}

\item{data_dir}{Directory holding the canonical Parquet store.}

\item{label}{Issue label used by the alert channel.}

\item{marker_prefix}{Namespace of the hidden deduplication marker.}
}
\value{
Invisibly, a tibble with one row per action and the columns
\code{kind}, \code{package}, \code{action} (\code{"created"}, \code{"commented"}, \code{"closed"} or
\code{"dry-run"}), \code{issue} (issue number, \code{NA} for a dry run) and \code{title}.
}
\description{
Reconciles the repository's open \code{alert} issues against the latest CRAN
snapshot in \code{data_dir}, following SPEC.md S8 exactly:
\itemize{
\item \code{worst_status} in \code{WARN}/\code{ERROR}/\code{FAIL} opens
\verb{[CRAN] \{pkg\}: \{status\} em \{n\} flavor(s)}, high severity. While the
condition persists the issue is updated by a comment, never reopened
and never duplicated. It is closed automatically as soon as the worst
status returns to \code{OK} or \code{NOTE}.
\item \code{archived = TRUE} opens a critical issue that is \strong{never} closed
automatically; it waits for a human.
}

Deduplication is by a hidden HTML marker in the issue body, one per
\verb{(kind, package)} -- \verb{<!-- zboard:cran-check:\{pkg\} -->} or
\verb{<!-- zboard:archived:\{pkg\} -->}. Open issues are listed and
matched on that marker before anything is created, so a second issue is
never opened for a condition that is already tracked.
}
\details{
\strong{Dry run.} When \code{token} is \code{""} -- no PAT in the environment, a fork,
a local run -- nothing is sent to the API. The plan is printed and the
function returns invisibly without error, so a workflow without
\code{issues: write} does not fail (SPEC.md S11 restricts that permission to
the \code{alert} job).

The token is never printed and never written anywhere.
}
\examples{
\dontrun{
# In the `alert` job of the workflow.
run_alerts(
  repo = Sys.getenv("GITHUB_REPOSITORY"),
  token = Sys.getenv("GITHUB_TOKEN")
)
}

# Without a token: prints the plan, touches no API.
run_alerts(repo = "evandeilton/zboard", token = "", data_dir = tempfile())
}
\seealso{
\code{\link[=consolidate]{consolidate()}}
}
