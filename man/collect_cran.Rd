% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collect_cran.R
\name{collect_cran}
\alias{collect_cran}
\title{Collect CRAN health, version and download data}
\usage{
collect_cran(
  from = NULL,
  to = NULL,
  config_path = "config/repos.yml",
  staging_dir = "staging",
  run_ts = Sys.time()
)
}
\arguments{
\item{from}{Start of the download window as an ISO-8601 date string or a
\code{Date}. \code{NULL} (the default) means normal incremental mode: a rolling
35-day window ending yesterday. The window is deliberately wider than
one day so that the series self-heals after an outage.}

\item{to}{End of the download window, same forms as \code{from}. \code{NULL}
defaults to yesterday, because the current UTC day is still incomplete
upstream.}

\item{config_path}{Path to the curated source configuration.}

\item{staging_dir}{Directory the staging Parquet files are written to.
Created when missing.}

\item{run_ts}{Timestamp shared by every manifest row of this run.}
}
\value{
Invisibly, a tibble with the \code{run_manifest} schema: one row per
source attempted, with \code{status} in \code{ok}/\code{partial}/\code{failed},
\code{rows_written}, \code{duration_s} and a sanitised \code{message}. The same rows
are written to \code{staging_dir/run_manifest_cran.parquet}.
}
\description{
Collects the four CRAN-side tables for the packages
curated in \code{config/repos.yml}, and writes each one as a Parquet file in
\code{staging_dir} for \code{\link[=consolidate]{consolidate()}} to upsert:
\itemize{
\item \code{cran_downloads} -- daily download counts from the Posit mirror
(\code{cranlogs.r-pkg.org}). Retroactive, so \code{from}/\code{to} apply here.
\item \code{cran_versions} -- the full published version history and release
dates from \code{crandb.r-pkg.org/{pkg}/all}.
\item \code{cran_checks} -- \verb{R CMD check} status per flavour, read from CRAN's
official \code{.rds} artifact via \code{tools::CRAN_check_results()}.
\item \code{cran_status} -- presence on CRAN, archival state, maintainer and the
worst check status across flavours.
}

Each of the four is collected independently: a failure in one is
recorded in the run manifest and does not prevent the others from being
written. Every network call retries three times with exponential
backoff on 5xx/timeout and honours \code{Retry-After} on 429.
}
\details{
Only \code{cran_downloads} is retroactive. \code{cran_checks} and \code{cran_status} are
snapshots of CRAN's \emph{current} state and \code{cran_versions} is a complete
history in every run, so an explicit \code{from}/\code{to} window is applied to the
download series only; the other three are unaffected (and a message says
so).

The e-mail address in \code{cran_status$maintainer} is collected as CRAN
publishes it; \code{\link[=export_json]{export_json()}} strips it before the column reaches a
public JSON file.
}
\examples{
\dontrun{
# Normal daily run.
collect_cran()

# Backfill twelve months of the download series.
collect_cran(from = "2025-09-01", to = "2026-08-31")
}
}
\seealso{
\code{\link[=collect_github]{collect_github()}}, \code{\link[=collect_academic]{collect_academic()}}, \code{\link[=consolidate]{consolidate()}}
}
